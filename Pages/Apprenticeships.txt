import React, { useState } from "react";
import { base44 } from "@/api/base44Client";
import { useQuery } from "@tanstack/react-query";
import { Card } from "@/components/ui/card";
import { GraduationCap } from "lucide-react";
import { Link } from "react-router-dom";
import { createPageUrl } from "@/utils";
import { Button } from "@/components/ui/button";

export default function Apprenticeships() {
  const { data: apprenticeships, isLoading } = useQuery({
    queryKey: ['apprenticeships'],
    queryFn: () => base44.entities.JobOpportunity.filter({ 
      job_type: "Apprenticeship",
      active: true 
    }, '-created_date'),
    initialData: []
  });

  return (
    <div className="min-h-screen bg-gray-50">
      {/* Header */}
      <div className="bg-gradient-to-br from-violet-500 to-violet-600 text-white">
        <div className="max-w-7xl mx-auto px-4 py-12">
          <div className="flex items-center gap-3 mb-4">
            <div className="w-14 h-14 bg-white/20 backdrop-blur-sm rounded-2xl flex items-center justify-center">
              <GraduationCap className="w-7 h-7" />
            </div>
            <div>
              <h1 className="text-3xl md:text-4xl font-bold">Apprenticeship Portal</h1>
              <p className="text-violet-100 mt-1">National Apprenticeship Training Scheme (NATS)</p>
            </div>
          </div>

          <div className="bg-white/10 backdrop-blur-sm rounded-xl p-4 border border-white/20">
            <p className="text-violet-50 text-sm">
              🎓 Explore government-approved apprenticeship opportunities across India
            </p>
          </div>
        </div>
      </div>

      <div className="max-w-7xl mx-auto px-4 py-8">
        <Card className="p-6 mb-6">
          <h2 className="text-xl font-semibold mb-4">About Apprenticeships</h2>
          <p className="text-gray-600 mb-4">
            The National Apprenticeship Training Scheme (NATS) provides on-the-job training to youth while providing them a stipend. 
            Apprenticeships combine practical work experience with structured training, helping young people develop skills for their future careers.
          </p>
          <div className="grid md:grid-cols-3 gap-4 mt-6">
            <div className="bg-violet-50 p-4 rounded-lg">
              <h3 className="font-semibold text-violet-900 mb-2">Duration</h3>
              <p className="text-sm text-violet-700">Typically 6 months to 3 years</p>
            </div>
            <div className="bg-violet-50 p-4 rounded-lg">
              <h3 className="font-semibold text-violet-900 mb-2">Stipend</h3>
              <p className="text-sm text-violet-700">Monthly stipend as per rules</p>
            </div>
            <div className="bg-violet-50 p-4 rounded-lg">
              <h3 className="font-semibold text-violet-900 mb-2">Certificate</h3>
              <p className="text-sm text-violet-700">Nationally recognized certification</p>
            </div>
          </div>
        </Card>

        <div className="mb-4 flex justify-between items-center">
          <p className="text-gray-600">
            <span className="font-semibold text-gray-900">{apprenticeships.length}</span> apprenticeship opportunities available
          </p>
          <Link to={createPageUrl("JobsCareer")}>
            <Button variant="outline">View All Jobs</Button>
          </Link>
        </div>

        {isLoading ? (
          <div className="text-center py-12">
            <div className="animate-spin rounded-full h-12 w-12 border-b-2 border-violet-600 mx-auto" />
          </div>
        ) : apprenticeships.length > 0 ? (
          <div className="space-y-4">
            {apprenticeships.map(apprenticeship => (
              <Card key={apprenticeship.id} className="p-6 hover:shadow-lg transition-all">
                <div className="flex justify-between items-start">
                  <div>
                    <h3 className="text-xl font-semibold text-gray-900">{apprenticeship.job_title}</h3>
                    <p className="text-gray-600 mt-1">{apprenticeship.organization}</p>
                    <p className="text-sm text-gray-500 mt-2">{apprenticeship.description}</p>
                  </div>
                  {apprenticeship.application_link && (
                    <Button 
                      className="bg-violet-600 hover:bg-violet-700 text-white"
                      onClick={() => window.open(apprenticeship.application_link, '_blank')}
                    >
                      Apply Now
                    </Button>
                  )}
                </div>
              </Card>
            ))}
          </div>
        ) : (
          <div className="text-center py-12">
            <GraduationCap className="w-16 h-16 text-gray-300 mx-auto mb-4" />
            <p className="text-gray-500 text-lg">No apprenticeships available at the moment</p>
            <p className="text-gray-400 text-sm mt-2">Check back soon for new opportunities</p>
          </div>
        )}
      </div>
    </div>
  );
}